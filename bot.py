import asyncio
import random
from aiogram import Bot, Dispatcher, types

TOKEN = "8352418610:AAETBXF8DoIOlgjKUcB11a66aHDwlDupzOY"

bot = Bot(token=TOKEN)
dp = Dispatcher()

# Рандомные фразы при старте бота
start_messages = [
    "🔥 Бот проснулся и готов творить магию!",
    "🤖 Я тут, заряжен и готов к приключениям!",
    "⚡ Запуск успешен. Илля, что делаем?",
    "🧠 Мозги прогреты. Погнали!"
]

# Рандомные ответы на первое сообщение
first_replies = [
    "Ого, ты первый сегодня! Что расскажешь?",
    "Ну привет! Я уже ждал тебя.",
    "Залетел как к себе домой. Что делаем?",
    "Я тут, я тут! Пиши, не стесняйся."
]

# Флаг, чтобы знать, что это первое сообщение
first_message = True

@dp.message()
async def handle_message(message: types.Message):
    global first_message

    if first_message:
        first_message = False
        await message.answer(random.choice(first_replies))
        return

    # Дальше бот отвечает не повторяя текст
    responses = [
        "Интересно звучит.",
        "Хмм… любопытно.",
        "А вот это уже что‑то новенькое.",
        "Записал в мозг. Что дальше?",
        "Окей, принял к сведению."
    ]

    await message.answer(random.choice(responses))

async def main():
    print(random.choice(start_messages))  # Прикольный вывод в консоль при старте
    await dp.start_polling(bot)

if __name__ == "__main__":
    asyncio.run(main())